#!/usr/bin/env zsh
dir=$PROJECT_DIR/versioned/paper
cd $dir

search_dirs=(../sections includes ../../analysis ../graphics/build ./text)

figurator collect --copy text/includes.yaml collected ${search_dirs}

